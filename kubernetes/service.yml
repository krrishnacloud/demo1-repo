apiVersion: v1
kind: Service
metadata:
  name: web-portfolio
spec:
  selector:
    app: web-portfolio
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      nodeport: 30020
  type: NodePort
