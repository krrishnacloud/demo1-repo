apiVersion: v1
kind: Service
metadata:
  name: web-portfolio
spec:
  selector:
    app: web-portfolio
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30080
  type: LoadBalancer
